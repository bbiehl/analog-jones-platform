import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class EpisodeListService {
  // Get a list of episodes for each genre, sorted by release date (newest to oldest).
}
