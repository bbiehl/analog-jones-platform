export interface EpisodeCategory {
  episodeId: number;
  categoryId: number;
}

export interface EpisodeGenre {
  episodeId: number;
  genreId: number;
}

export interface EpisodeTag {
  episodeId: number;
  tagId: number;
}
