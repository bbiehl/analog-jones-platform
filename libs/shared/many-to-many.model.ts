export interface EpisodeCategory {
  episodeId: string;
  categoryId: string;
}

export interface EpisodeGenre {
  episodeId: string;
  genreId: string;
}

export interface EpisodeTag {
  episodeId: string;
  tagId: string;
}
