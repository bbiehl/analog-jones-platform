runConfig:
  minInstances: 0
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 512

buildConfig:
  rootDir: /
  buildCommand: pnpm run build:public

env:
  - variable: ANGULAR_PROJECT_NAME
    value: public-app
    availability:
      - BUILD
