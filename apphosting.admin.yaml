runConfig:
  minInstances: 0
  maxInstances: 3
  concurrency: 80
  cpu: 1
  memoryMiB: 256

buildConfig:
  rootDir: /
  buildCommand: pnpm run build:admin

env:
  - variable: ANGULAR_PROJECT_NAME
    value: admin-app
    availability:
      - BUILD
